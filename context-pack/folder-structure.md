# Final Folder Structure Standard (SRC-BASED)

Youtify/
└── src/
    ├── app/              # Next.js App Router (Layouts & Pages)
    ├── modules/          # Layered Backend Modules (Controller/Service/Repo)
    ├── components/       # UI Components (Auth, Dashboard, Shared)
    ├── lib/              # Shared logic (Database connection, JWT helpers)
    └── scripts/          # CLI Utility scripts (Seed root user)
